name: Test this action 

on:
  push:
  workflow_dispatch:

jobs:
  test-action:
    runs-on: ubuntu-latest

    steps:
      - name: test action
        uses: jeroenknoops/github-actions-docker-with@main
        with:
          argument1: "argument1"
